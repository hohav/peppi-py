[project]
name = "peppi-py"
dependencies = [
	"pyarrow~=15.0",
	"inflection~=0.5",
]

[build-system]
build-backend = "maturin"
requires = ["pyarrow~=15.0"]

[tool.maturin]
module-name = "peppi_py._peppi"

[pytest]
testpaths = "tests"
