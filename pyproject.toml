[project]
name = "peppi-py"
dependencies = [
	"pyarrow~=16.1",
	"inflection~=0.5",
]

[build-system]
build-backend = "maturin"
requires = ["pyarrow~=16.1"]

[tool.maturin]
module-name = "peppi_py._peppi"

[pytest]
testpaths = "tests"
